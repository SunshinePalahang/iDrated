<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="0dp">

    <!-- ImageView for the hydration tip -->
    <com.airbnb.lottie.LottieAnimationView
        android:id="@+id/healthTipAnimation"
        android:layout_width="match_parent"
        android:layout_height="385dp"
        android:layout_marginBottom="20dp"
        app:lottie_rawRes="@raw/hydration_tip"
        app:lottie_autoPlay="true"
        app:lottie_loop="true"/>

    <!-- TextView for the hydration tip -->
    <TextView
        android:id="@+id/hydrationTipText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:fontFamily="@font/poppins"
        android:gravity="center"
        android:text="Hydration Tip will appear here"
        android:textSize="22sp"
        android:textStyle="bold" />
</LinearLayout>
